import { createSlice } from '@reduxjs/toolkit' //帮助我们创建对应的片段

const homeSlice = createSlice({
  name: 'home',
  initialState: { //初始化数据，但是现在还不知道需要放什么数据进去

  },
  reducers: {

  }

})

export default homeSlice.reducer