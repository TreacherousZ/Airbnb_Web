import styled from "styled-components";

export const LongForWrapper = styled.div`
    margin-top: 30px;

    .longfor-list{
         display: flex;
         margin:  0 -8px; //让最左右两张没有左右顶齐，和上下文档对齐
    }
    
`