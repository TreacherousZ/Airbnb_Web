import styled from "styled-components"

export  const HomeWrapper = styled.div`
    > .content { 
        width: 1032px;
        margin: 0 auto;

        > div{   // content里面的直接子元素div
            margin-top: 30px;
        }
    }
`